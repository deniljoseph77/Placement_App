import 'package:flutter/material.dart';

class TPOManageRequestScreen extends StatelessWidget {
  const TPOManageRequestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Manage Request")),
    );
  }
}
